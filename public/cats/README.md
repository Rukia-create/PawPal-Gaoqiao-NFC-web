# Cat Assets

Real cat cutout/photo images go in `real/`.

Comic cat images go in `comic/`.

Use filenames that match point ids:

```text
real/cat-island.png
comic/cat-island.png
```

If an image is missing, the app falls back to `/pawpal/pawpal-placeholder.png`.
